language: python

jobs:
    allow_failures:
        - os: osx
    include:
        # Use generic language for osx
        - os: osx
          language: generic
          env: TOXENV=py36
      
        - os: osx
          language: generic
          env: TOXENV=py37

before_install:
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; fi
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew list python &>/dev/null || brew install python; fi
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew list python3 &>/dev/null || brew install python3; fi
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install pyenv-virtualenv; fi
  # Here we just install Miniconda, which you shouldn't have to change.

- export MINICONDA=$HOME/miniconda
- export PATH="$MINICONDA/bin:$PATH"
- hash -r
#- wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
- wget --no-check-certificate https://repo.anaconda.com/miniconda/Miniconda2-latest-MacOSX-x86_64.sh -O miniconda.sh
- bash miniconda.sh -b -f -p $MINICONDA
- conda config --set always_yes yes
- conda update conda
- conda info -a
- echo "Python version var"
- echo $TRAVIS_PYTHON_VERSION
- conda create -n xesmf_env python=$TRAVIS_PYTHON_VERSION
- source activate xesmf_env

install:
- conda install -c conda-forge xesmf
- conda install -c conda-forge dask netCDF4
- conda install -c conda-forge xesmf esmpy=7.1.0
#- conda install --yes --file requirements.txt

script: python setup.py test

